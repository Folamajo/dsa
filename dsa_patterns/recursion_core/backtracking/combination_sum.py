def combination_sum(candidates: list[int], target:int) -> list[list[int]]:
   output = []
   def helper(path: list[int], start : int, target: int):
      path_sum = sum(path)
      if path_sum == target:

      if path_sum> target: 

